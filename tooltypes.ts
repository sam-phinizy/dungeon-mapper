export enum ToolType {
  PEN = "pen",
  RECT = "rect",
  CIRCLE = "circle",
  LINE = "line",
  SELECT = "select",
  DOOR = "door",
  // NOTES = "notes",
  ROUGH_LINE = "roughLine",
  // DOWNLOAD = "download",
  // CLEAR_MAP = "clearMap",
  COLOR = "color",
}
